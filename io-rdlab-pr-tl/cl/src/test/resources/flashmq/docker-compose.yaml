services:
  flashmq:
    image: privatecoder/flashmq:v1.22.0
    container_name: flashmq_broker
    ports:
      - "1883:1883" # Standard MQTT port
      - "8883:8883" # MQTT over TLS
    volumes:
      - ./flashmq.conf:/etc/flashmq/flashmq.conf:ro
    restart: unless-stopped
